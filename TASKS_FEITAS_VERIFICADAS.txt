# Tasks feitas e verificadas

Data da revisao: 2026-06-10
Branch local revisada: `main`
Reviewer documentado nas branches: `licori12`

## Branches integradas em `main`

### `feature/frontend-tests-xss-clean`

Status: feita e mergeada em `main`.

Tasks concluidas:
- Adicionar testes ao frontend com Vitest.
- Adicionar testes para helpers de API/UI/sanitizacao.
- Aplicar sanitizacao XSS em pontos que renderizam dados via `innerHTML`.

Arquivos principais:
- `frontend/vitest.config.js`
- `frontend/assets/js/api.test.js`
- `frontend/assets/js/ui.test.js`
- `frontend/assets/js/sanitize.test.js`
- `frontend/assets/js/sanitize.js`
- `frontend/assets/js/page-behaviors.js`
- `frontend/assets/js/shell.js`
- `frontend/assets/js/ui.js`

Validacao:
- `npm test --workspace frontend -- --run`: passou com 34 testes.
- `npm run build --workspace frontend`: passou.

Observacao:
- A cobertura de API ainda pode ser melhorada com testes diretos de parsing do envelope de sucesso/erro.

### `feature-add-eslint-and-prettier-configuration-with-documentation`

Status: feita e mergeada em `main`.

Tasks concluidas:
- Adicionar ESLint.
- Adicionar Prettier.
- Adicionar scripts de lint/format no workspace frontend e raiz.
- Adicionar documentacao de lint em `frontend/LINTING.md`.

Arquivos principais:
- `frontend/eslint.config.js`
- `frontend/.prettierrc`
- `frontend/LINTING.md`
- `frontend/package.json`
- `package.json`
- `package-lock.json`

Validacao:
- `npm run lint:check --workspace frontend`: passou.
- `npm test --workspace frontend -- --run`: passou com 34 testes.
- `npm run build --workspace frontend`: passou.

### `backend-dev-2-auth-router`

Status: feita e mergeada em `main`.

Tasks concluidas:
- Adicionar middleware de autenticacao por bearer token.
- Proteger rotas autenticadas no backend.
- Manter login publico.
- Retornar 401 quando token esta ausente ou invalido.
- Adicionar suporte a middleware no router.
- Disponibilizar usuario autenticado no `Request`.

Arquivos principais:
- `backend/routes/api.php`
- `backend/src/Core/Middleware/AuthMiddleware.php`
- `backend/src/Core/Router.php`
- `backend/src/Core/Request.php`
- `backend/src/Core/RequestInterface.php`
- `backend/src/Core/Response.php`
- `backend/src/Services/AuthService.php`
- `backend/src/Services/AuthServiceInterface.php`
- `backend/tests/Middleware/AuthMiddlewareTest.php`
- `backend/tests/smoke.sh`

Validacao:
- `php -l` nos arquivos PHP de `backend/src`, `backend/routes` e `backend/tests`: passou.
- Teste direto do middleware sem token: retornou 401.
- `npm run lint:check --workspace frontend`: passou.
- `npm test --workspace frontend -- --run`: passou com 34 testes.
- `npm run build --workspace frontend`: passou.

### `feature-rate-limit`

Status: feita e mergeada em `main`.

Tasks concluidas:
- Criar `backend/src/Support/RateLimit.php`.
- Integrar rate limiting no login em `backend/src/Controllers/AuthController.php`.
- Adicionar `Response::tooManyRequests()` com status 429.
- Limitar login a 5 tentativas falhadas por 5 minutos por email.
- Registrar log das tentativas bloqueadas.
- Adicionar teste smoke especifico para 5 falhas seguidas de 429 na 6a tentativa.

Arquivos principais:
- `backend/src/Support/RateLimit.php`
- `backend/src/Controllers/AuthController.php`
- `backend/src/Core/Response.php`
- `backend/tests/smoke.sh`

Validacao:
- `php -l backend/src/Support/RateLimit.php`: passou.
- `php -l backend/src/Controllers/AuthController.php`: passou.
- `php -l backend/src/Core/Response.php`: passou.
- `API=http://localhost:8080 bash backend/tests/smoke.sh`: passou; cobre 5 tentativas invalidas com 401 e a 6a com 429.
- `git diff --check -- backend/tests/smoke.sh`: passou.

## Ajuste complementar

### Docker healthcheck do frontend

Status: feito.

Task/ajuste:
- Corrigir healthcheck do container `web` para usar `http://127.0.0.1/` em vez de `http://localhost/`.

Arquivo:
- `frontend/Dockerfile`

Validacao Docker:
- `DOCKER_BUILDKIT=0 docker compose up -d --build`: passou.
- `docker compose ps`: `db` healthy, `api` up, `web` healthy.
- `curl http://localhost:8080/health`: API healthy.
- `curl http://localhost:4173/`: HTTP 200.
- `API=http://localhost:8080 bash backend/tests/smoke.sh`: 20 passed, 0 failed.

### Helper reutilizavel de render de tabela

Status: feito em `feature/table-render-helper`.

Task/ajuste:
- Extrair helpers compartilhados para renderizar estados e linhas de tabelas do frontend.
- Aplicar em inventario, pacientes e pedidos.
- Adicionar testes unitarios para os helpers.

Arquivos:
- `frontend/assets/js/ui.js`
- `frontend/assets/js/page-behaviors.js`
- `frontend/assets/js/ui.test.js`
- `PROJECT_CONTEXT.md`
- `TASKS_COM_RESPONSAVEIS.md`

### Topbar global search e acoes

Status: feito em `feature/topbar-global-actions`.

Task/ajuste:
- Implementar busca global da topbar para medicamentos, pacientes e prescricoes.
- Ligar os botoes Notifications e Help, antes decorativos.
- Adicionar testes unitarios para renderizacao e interacoes da topbar.

Arquivos:
- `frontend/assets/js/shell.js`
- `frontend/assets/js/shell.test.js`
- `frontend/assets/css/app.css`
- `PROJECT_CONTEXT.md`
- `TASKS_COM_RESPONSAVEIS.md`

### Cobertura de parsing do cliente de API

Status: feito em `feat-frontend-api-test`.

Task/ajuste:
- Completar cobertura de `api.js` para parsing de envelope de sucesso/erro.
- Adicionar cobertura para falha de rede com `ApiError` status 0.

Arquivo:
- `frontend/assets/js/api.test.js`

Validacao:
- `npm test --workspace frontend -- --run`: passou com 44 testes.
- `npm run lint:check --workspace frontend`: passou.
- `npm run build --workspace frontend`: passou.

### Analise de CSS potencialmente nao usado

Status: feito em `cssInutil`.

Task/ajuste:
- Rodar analise PurgeCSS em dry-run para avaliar remocao segura de CSS potencialmente nao usado.
- Registrar decisao de nao remover regras automaticamente, porque a diferenca observada foi pequena e a cobertura estatica pode nao capturar classes dinamicas.

Arquivo:
- `frontend/assets/css/purgecss.config.cjs`

Validacao/conclusao:
- Analise reportada na branch: CSS original com 21186 caracteres e CSS processado com 20946 caracteres.
- Diferenca aproximada de 1%; remocao automatica considerada insegura neste momento.
- `npm test --workspace frontend -- --run`: passou com 44 testes.
- `npm run lint:check --workspace frontend`: passou.
- `npm run build --workspace frontend`: passou.

### Logging estruturado de erros

Status: feito em `feature/backend-error-logging`.

Task/ajuste:
- Registrar excecoes internas 500 em formato JSON estruturado.
- Preservar a resposta HTTP 500 existente, incluindo detalhes somente quando `APP_DEBUG=true`.

Arquivo:
- `backend/src/Core/App.php`

Dados registrados:
- `timestamp`
- `level`
- `message`
- `exception`
- `file`
- `line`
- `trace`

## Ainda pendente

- Pipeline de CI.
- Resolver o parametro `reason` do ajuste de estoque.
- Checagem proativa de expiracao do token no frontend.
- Feedback offline/retry de rede.
- Externalizar identidade da filial.
- Focus trap em modais.
- `aria-live` em toasts/estados.
- Loading state e prevencao de duplo-submit nos forms.
- Paginacao backend/frontend.
- Limpeza de wrappers sem uso em `api.js`.
- Endpoints sem consumidor de UI.
- Suite backend mais completa com PHPUnit (o `composer.json` ja lista PHPUnit e ha `AuthMiddlewareTest`, mas falta uma suite abrangente para services/validator e execucao documentada).
